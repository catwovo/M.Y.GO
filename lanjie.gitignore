# ==================== Godot 4 忽略清单 ====================
# 忽略 Godot 4 自动生成的本地导入、索引和着色器缓存（体积巨大且每天都在变，绝不需要上传）
.godot/

# ==================== 外部大文件拦截 (防止 GitHub 100MB 拦截报错) ====================
# 忽略二进制大引擎和几十MB的神经网络权重（建议让合作者/玩家自行下载 bin/ 放入同级目录）
bin/katago.exe
bin/*.bin.gz
bin/*.gz
bin/*.dll
bin/*.zip
bin/*.cfg